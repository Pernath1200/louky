/**
 * Louky UI lives under `src/louky/`. Routes are nested under `path="louky"` in `App.tsx`
 * so React Router v7 matches `/louky/*` in a single route tree.
 */
export type { LoukyOutletContext } from './types'
